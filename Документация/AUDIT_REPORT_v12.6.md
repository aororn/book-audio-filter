# Глубокий аудит проекта Яндекс Спич v12.6

**Дата:** 2026-01-30
**Версия проекта:** 12.6.0
**Golden:** 127/127 (100%)
**FP:** 327 (из 454 ошибок)

---

## Резюме аудита

### Текущее состояние
- **Архитектура:** Зрелая, модульная, но требует рефакторинга
- **Логика фильтрации:** Консервативная, эффективная на 72%
- **Код:** 25,000+ строк, циклическая сложность HIGH в engine.py
- **Потенциал улучшения:** -80 FP без потери golden

### Ключевые находки

| Категория | Критичных | Важных | Низких |
|-----------|-----------|--------|--------|
| Архитектура | 3 | 5 | 4 |
| Логика фильтрации | 2 | 4 | 3 |
| Пороговые значения | 1 | 3 | 2 |
| Паттерны FP | 0 | 4 | 2 |

---

## ЧАСТЬ 1: АРХИТЕКТУРА И МОДУЛЬНОСТЬ

### 1.1 Критические проблемы

#### 🔴 P1: Монолитная функция should_filter_error() — 656 строк
**Файл:** `engine.py` (строки 238-891)
**Проблема:** Невозможно тестировать, отлаживать, понимать
**Рекомендация:** Разбить на подфункции по уровням фильтрации:
```python
def should_filter_error(error, config=None):
    # Подготовка
    ctx = _prepare_filter_context(error)

    # Защитные слои
    result = _apply_protection_layers(ctx)
    if result: return result

    # Морфология
    result = _apply_morpho_rules(ctx)
    if result: return result

    # ... остальные уровни
```

#### 🔴 P2: Логическая ошибка в check_alignment_artifact
**Файл:** `engine.py` (строки 351-360)
**Проблема:** Условие `if should_filter` выполняется ВНЕ блока `if not skip_alignment`
```python
# ТЕКУЩЕЕ (ОШИБКА):
if not skip_alignment:
    should_filter, reason = check_alignment_artifact(...)
if should_filter:  # ← Всегда True если skip_alignment=False!
    return True, reason

# ПРАВИЛЬНОЕ:
if not skip_alignment:
    should_filter, reason = check_alignment_artifact(...)
    if should_filter:
        return True, reason
```

#### 🔴 P3: Дублирование защиты merged_from_ins_del (3 места)
**Файлы:** `engine.py` (строки 343-349, 786-791, 870-876)
**Рекомендация:** Вынести в функцию:
```python
def should_skip_merged_error(error: Dict, words_norm: List[str]) -> bool:
    if not error.get('merged_from_ins_del', False):
        return False
    if len(words_norm) < 2:
        return False
    lemma1, lemma2 = get_lemma(words_norm[0]), get_lemma(words_norm[1])
    return lemma1 and lemma2 and lemma1 != lemma2
```

### 1.2 Важные проблемы

#### 🟡 P4: 7 условных импортов создают 128 code paths
**Файл:** `engine.py` (строки 172-223)
```python
HAS_SCORING_ENGINE = ...
HAS_SEMANTIC_MANAGER = ...
HAS_SMART_SCORER = ...
HAS_SMART_FILTER = ...      # ОТКЛЮЧЁН
HAS_CONTEXT_VERIFIER = ...
HAS_ML_CLASSIFIER = ...
HAS_RULES_MODULE = ...
```
**Рекомендация:** Создать `FilterCapabilities` dataclass

#### 🟡 P5: Overgeneralization compound_particle без word boundaries
**Файл:** `engine.py` (строки 414-440)
```python
# ТЕКУЩЕЕ:
if pattern in context:  # "как то" найдётся в "вкусного то"!

# ПРАВИЛЬНОЕ:
import re
if re.search(r'\b' + prefix + r'\s+то\b', context, re.IGNORECASE):
```

#### 🟡 P6: Противоречивая логика падежей
**Файлы:** `comparison.py` vs `morpho_rules.py`
- `comparison.py`: фильтрует безопасные пары (им.↔вин.)
- `morpho_rules.py`: НЕ фильтрует любые разные падежи
**Рекомендация:** Унифицировать в одном месте

#### 🟡 P7: Мёртвый код в comparison.py
**Функции (не используются):**
- `is_grammar_ending_match()` (строки 207-227)
- `is_adverb_adjective_match()` (строки 285-308)
- `is_verb_gerund_safe_match()` (строки 311-341)

#### 🟡 P8: phonetic_normalize слишком агрессивен
**Файл:** `comparison.py` (строки 558-583)
```python
# Правила е→и, я→и слишком широки
# "делаю" → "диллау", "заявка" → "зиявка"
```
**Рекомендация:** Ограничить только корневые безударные

### 1.3 Низкоприоритетные проблемы

- P9: Дублирование `cache_info()` вызовов (строки 1104, 1165)
- P10: Нормализация контекста через `isalpha` (строка 517-520)
- P11: Повторяющиеся морфологические разборы (строки 298-303, 800-803)
- P12: `calculate_smart_score()` создаёт объект для каждой ошибки

---

## ЧАСТЬ 2: ЛОГИКА ФИЛЬТРАЦИИ

### 2.1 Context Verifier v4.0 — Критический анализ

#### Эффективность по уровням:

| Уровень | Метод | FP | Статус |
|---------|-------|-----|--------|
| L1 | anchor_verification | 4 | 🔴 BROKEN |
| L2 | morpho_coherence | 3 | 🟡 Консервативно |
| L3 | semantic_coherence | 0 | 🟡 Переобучено |
| L4 | phonetic_morphoform | 33 | 🟡 Неполная защита |

#### 🔴 L1: Логика insertion СЛОМАНА
**Файл:** `context_verifier.py` (строки 146-154)
```python
# ТЕКУЩЕЕ (ОШИБКА):
if trans_words and suffix in trans_words:  # Не учитывает позицию!

# ПРАВИЛЬНОЕ:
# Проверить, что суффикс находится РЯДОМ с исходным словом
```

#### 🟡 L3: Пороги переобучены на 7 примерах
```python
# v3.1: "7 FP отфильтровано без потери golden"
# Это слишком маленькая выборка для надёжных порогов
if sim_diff > 0.15 and trans_sim > 0.25 and orig_sim < 0.35:
```

#### 🟡 L4: Неполный список защиты
**Пропущены пары:**
- `одеяния ↔ одеяние` (одинаковая фонетика, не защищена!)
- `магистра ↔ магистр` (фонетическая нормализация не работает)

### 2.2 Scoring Engine — Анализ

#### HARD_NEGATIVES очищен (правильно)
```python
HARD_NEGATIVES = {
    'джейра': 'вчера',  # Только 1 пара (имя персонажа)
}
```
**Статус:** ✅ Корректно — грамматика обрабатывается morpho_rules

#### SemanticManager — Эффективен
- Порог 0.5 — консервативный
- 77 ошибок защищено от ложной фильтрации
- Lazy loading экономит память

### 2.3 Rules/ модули — Анализ

#### Эффективность:
| Правило | Golden | FP | Статус |
|---------|--------|-----|--------|
| SAFE_ENDING_TRANSITIONS | 0 | 85 | ✅ Отличный |
| single_consonant | 0 | 47 | ✅ Хороший |
| YANDEX_PHONETIC_PAIRS | 1 | 0 | ⚠️ Редко срабатывает |
| alignment_artifact_length | 0 | 0 | ❓ Не срабатывает |

#### 🟡 Дублирование PHONETIC_SLIP_THRESHOLD
**Файлы:** `protection.py` + `engine.py`
**Проблема:** Константа объявлена в двух местах, не используется

---

## ЧАСТЬ 3: АНАЛИЗ БД ОШИБОК

### 3.1 Общая статистика (942 записи)

| Метрика | Значение |
|---------|----------|
| Golden | 93 (9.9%) |
| FP отфильтровано | 715 (75.8%) |
| FP НЕ отфильтровано | 134 (14.2%) |

### 3.2 Главные паттерны НЕ отфильтрованных FP

| Паттерн | Количество | Потенциал фильтра |
|---------|------------|-------------------|
| **и → '' (insertion)** | 13 | 🔴 ВЫСОКИЙ |
| **→ же (deletion)** | 6 | 🔴 ВЫСОКИЙ |
| **а → '' (insertion)** | 2 | 🟡 СРЕДНИЙ |
| **→ бы (deletion)** | 2 | 🟡 СРЕДНИЙ |

### 3.3 Распределение по типам ошибок

| Тип | Всего | Фильтруется % |
|-----|-------|---------------|
| Substitution | 624 | 76.8% |
| Deletion | 232 | 85.8% |
| **Insertion** | 84 | **53.6%** ⚠️ |
| Transposition | 2 | 0% |

**Вывод:** Insertions — главный фокус улучшения

### 3.4 Проблемная Глава 3

| Глава | Фильтруется % |
|-------|---------------|
| 1 | 79.2% |
| 2 | 79.2% |
| **3** | **67.5%** ⚠️ |
| 4 | 76.9% |

---

## ЧАСТЬ 4: НОВЫЕ ПАТТЕРНЫ ФИЛЬТРАЦИИ

### 4.1 Служебные слова (QUICK WIN: +25 FP)

```python
def is_service_word_artifact(wrong, correct, error_type):
    """
    Служебные слова (союзы, частицы, предлоги)
    часто ошибочно добавляются/удаляются Яндексом
    """
    SERVICE_WORDS = {'и', 'а', 'в', 'не', 'о', 'по', 'же', 'ли', 'то', 'да', 'бы'}

    if error_type == 'insertion':
        return wrong.lower() in SERVICE_WORDS and correct == ''

    if error_type == 'deletion':
        return wrong == '' and correct.lower() in SERVICE_WORDS

    return False
```

**Ожидаемый результат:** -25 FP, риск LOW

### 4.2 Adjective Gender Switch (+8 FP)

```python
def is_adjective_gender_switch(wrong, correct, pos_wrong, pos_correct):
    """
    неба → небо, лицо → лица (переход рода)
    """
    if pos_wrong == 'ADJF' and pos_correct == 'ADJF':
        if wrong.endswith('а') and correct.endswith('о'):
            return True
        if wrong.endswith('о') and correct.endswith('а'):
            return True
    return False
```

**Ожидаемый результат:** -8 FP, риск MEDIUM

### 4.3 Context Window для Anchor Verification (+10 FP)

```python
def verify_with_position(word, context_words, max_distance=2):
    """
    Проверяет, что артефакт находится рядом с исходным словом
    """
    for i, ctx_word in enumerate(context_words):
        if word in ctx_word or ctx_word in word:
            # Проверить соседние слова
            neighbors = context_words[max(0,i-max_distance):i+max_distance+1]
            # ... логика проверки
```

### 4.4 Расширение SAFE_ENDING_TRANSITIONS (+15 FP)

Добавить переходы:
- `-ства → -ство` (богатства → богатство)
- `-ения → -ение` (внимания → внимание)
- `-ия → -ие` (существования → существование)

---

## ДОРОЖНАЯ КАРТА РАБОТ

### Фаза 1: Архитектурный рефакторинг (2-3 дня)

| # | Задача | Приоритет | Сложность |
|---|--------|-----------|-----------|
| 1.1 | Исправить логическую ошибку в check_alignment_artifact | 🔴 P0 | EASY |
| 1.2 | Вынести should_skip_merged_error() в функцию | 🔴 P0 | EASY |
| 1.3 | Добавить word boundaries в compound_particle | 🔴 P1 | EASY |
| 1.4 | Удалить мёртвый код в comparison.py | 🟡 P2 | EASY |
| 1.5 | Удалить дублирование PHONETIC_SLIP_THRESHOLD | 🟡 P2 | EASY |
| 1.6 | Унифицировать логику падежей | 🟡 P2 | MEDIUM |

### Фаза 2: Оптимизация логики фильтрации (2-3 дня)

| # | Задача | Приоритет | Сложность |
|---|--------|-----------|-----------|
| 2.1 | Исправить L1 anchor_verification (позиция) | 🔴 P0 | MEDIUM |
| 2.2 | Расширить PROTECTED_MORPHO_PAIRS в L4 | 🔴 P1 | EASY |
| 2.3 | Переобучить пороги L3 на крупной выборке | 🟡 P2 | HARD |
| 2.4 | Исправить phonetic_normalize (е→и, я→и) | 🟡 P2 | MEDIUM |

### Фаза 3: Тестирование и калибровка (1-2 дня)

| # | Задача | Приоритет | Сложность |
|---|--------|-----------|-----------|
| 3.1 | A/B тест новых порогов на 942 ошибках | 🔴 P1 | MEDIUM |
| 3.2 | Cross-validation для ML порога | 🟡 P2 | MEDIUM |
| 3.3 | Анализ Главы 3 (низкий filter rate) | 🟡 P2 | EASY |
| 3.4 | Unit-тесты для всех защит | 🟢 P3 | MEDIUM |

### Фаза 4: Новые паттерны фильтрации (2-3 дня)

| # | Задача | Приоритет | Ожидаемый результат |
|---|--------|-----------|---------------------|
| 4.1 | Фильтр служебных слов | 🔴 P1 | -25 FP |
| 4.2 | Adjective gender switch | 🟡 P2 | -8 FP |
| 4.3 | Расширение SAFE_ENDING_TRANSITIONS | 🟡 P2 | -15 FP |
| 4.4 | Context window для anchor | 🟢 P3 | -10 FP |

### Фаза 5: Документация и финализация (1 день)

| # | Задача |
|---|--------|
| 5.1 | Обновить CLAUDE.md (v6.0) |
| 5.2 | Обновить PROJECT.md, ROADMAP.md, CHANGELOG.md |
| 5.3 | Разбить should_filter_error() на подфункции |
| 5.4 | Создать FilterCapabilities dataclass |
| 5.5 | Финальный golden тест |

---

## ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### После всех фаз:

| Метрика | До | После | Изменение |
|---------|-----|-------|-----------|
| Golden | 127/127 | 127/127 | ✓ Без потерь |
| FP | 327 | ~250 | **-77 FP** |
| Ошибок всего | 454 | ~377 | -17% |
| Код (строк в engine.py) | 1176 | ~800 | -32% |
| Циклическая сложность | ~40 | ~20 | -50% |

### Риски:

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| Потеря golden при новых фильтрах | LOW | A/B тест на каждое изменение |
| Регрессия производительности | LOW | Профилирование после рефакторинга |
| Несовместимость версий | MEDIUM | Версионирование в version.py |

---

## CHECKLIST ДЛЯ КАЖДОГО ИЗМЕНЕНИЯ

- [ ] Прогнать golden тест (127/127)
- [ ] Проверить, сколько FP затронуто
- [ ] Обновить VERSION в изменённых файлах
- [ ] Добавить запись в CHANGELOG.md
- [ ] Создать бэкап перед commit

---

*Автор: Claude Opus 4.5*
*Дата: 2026-01-30*
*Версия отчёта: 1.0*
